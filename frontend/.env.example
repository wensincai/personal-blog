# 前端环境变量示例：复制为 frontend/.env 并填入真实值（Vite 构建时注入 import.meta.env）
# 所有值均为占位符，切勿把真实值提交到仓库。

# 后端 API 基础地址（开发环境走 Vite 代理时可用 /api）
VITE_API_BASE_URL=http://localhost:8889/api

# SSO 中心登录地址（用于"SSO 登录"按钮跳转）
VITE_SSO_URL=http://localhost:9000
